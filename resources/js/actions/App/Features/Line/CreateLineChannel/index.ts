import CreateLineChannelController from './CreateLineChannelController'

const CreateLineChannel = {
    CreateLineChannelController: Object.assign(CreateLineChannelController, CreateLineChannelController),
}

export default CreateLineChannel